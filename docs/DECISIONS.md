# 判断記録

仕様変更、設計判断、採用・不採用理由を記録する。

## 記録書式

| 日付 | 変更内容 | 変更理由 | 影響範囲 | 再テスト項目 |
| --- | --- | --- | --- | --- |
| 2026-08-07 | Bゲート実行4として、full examined designの15層×2 pseudo-PSUを集計し、層を分割せず公開層コード順に3群へ循環割当する構造診断候補を確認した。候補は各群5層×2 PSU、PSU重複・未割当・single-PSU層0、課題×生コード×4入力セットの正のQC complete case件数を満たす。ただし方式は採用せず、3-way成立は未決定、モデル学習停止、A可・B/C保留を維持する。 | 採用理由は、結果や個人値を使わない決定的候補でPSU非重複と必要件数の存在を監査できるため。不採用理由は、各群が元15層中10層を欠き、分割後のdesign-based分散、空domain層、自由度、nested resampling、single-PSU処理の公式手法を未確認で、比率・fold・seedの採用根拠にならないため。 | 集計スクリプト、個人値を含まないmanifest、実現性文書、予測評価計画の結果参照、文書索引、本判断記録。生データ、個人行、モデル、係数、Webアプリ、ゲート状態は変更しない。 | 公式XPTからmanifest完全一致、SEQN一対一結合、15層×2 PSU、全30 PSUの一意割当、重複・未割当・個人跨ぎ0、群別層・PSU・自由度・single-PSU・欠落層、全課題・生コード・入力セット件数、禁止処理・禁止成果物の不存在、JSON・Markdown・リンク・diffを確認する。 |
| 2026-08-07 | Bゲート実行3として、ウエストとヒップを別課題、`RIAGENDR=1/2`を名称なしの別domain・別モデル候補とし、年齢と身長・体重・BMIの4入力セット、QC使用可能complete case、PSU非重複の学習・nested検証・封印最終試験、設計対応指標、加重中央値ベースライン、採用原則と停止条件を事前決定した。分割数値仕様、モデル探索、予測区間法、数値閾値は未確定とし、確定前の学習を停止する。 | 採用理由は、目的変数・生コード・QC状態・試験役割を混ぜず、複雑標本設計を反映して結果後の選択や閾値変更を防ぐため。不採用案は多出力化、他方の周囲長入力、`RIAGENDR`の説明変数化・結合、身長+体重+BMIの同時投入、個人ランダム分割、試験再利用、補完・値変更・独自外れ値処理、根拠のない数値閾値である。15層×2 PSUでの3-way分割とdesign-based分散の実現性を未検証のまま比率・fold数を捏造しない。 | 新規計画文書、採用判定基準、文書索引、本判断記録だけ。モデル、推定式、係数、データ、スクリプト、Webアプリ、胸囲、ANSUR II、A/B/C状態は変更しない。 | Markdownリンク・表、予測対象がウエスト/ヒップだけ、4入力セットに身長+体重+BMI同時投入なし、QC・設計・分割・指標・baseline・未確定閾値・停止条件、ゲート状態、禁止処理・個人行・生データの非収録、diffとクリーンな作業ツリーを確認する。 |
| 2026-08-06 | Bゲート実行2として、成人5項目の分析に `WTMEC2YR`、`SDMVSTRA`、`SDMVPSU` を使用し、full examined sampleでdesignを定義後に `RIDAGEYR >= 20`、`RIAGENDR` 生コード、項目別QC対象をdomain指定する方針を決定した。ウェイトは再正規化・加工せず、加重推定と非加重件数を分離する。 | 採用理由は、身体計測がMEC componentであり、CDC/NCHS公式資料がexam weight、層、PSUとsubpopulation analysisを指定するため。不採用案は `WTINT2YR`、equal/unweighted分析、層・PSUの無視、成人行の事前削除、欠損後の独自再正規化、`RIAGENDR` の無条件結合である。 | 方針文書、再現スクリプト、個票を含まないmanifest、採用判定基準、文書索引、本判断記録。値、既存QC、胸囲、ANSUR II、モデル、係数、Webアプリ、A/B/C状態は変更しない。 | 公式XPTからの完全一致再生成、3設計変数の存在・数値型・欠損・有効件数・正値、確認済みコード、成人と生コード別件数の加算、個人行・SEQN・個別ウェイト・生データ・仮想環境のGit非収録、JSON・Markdown・リンク・表・diffを確認する。 |
| 2026-08-06 | Bゲート実行1として、`RIDAGEYR >= 20` の5項目を対象に、測定不能と身体計測データなしを除外、理由未確認の欠損と品質comment付き有効値を保留、それ以外の非欠損値を使用可能とする暫定処理を決定した。BMIは身長・体重commentを連動する。補完、値変更、自動外れ値除外は行わず、`RIAGENDR` は生コード別のままとする。 | 採用理由は公式に意味を確認できたコードだけで再現可能な最小処理を定めるため。不採用案は、欠損の原因を推測すること、衣服・姿勢・medical applianceを仮係数で補正または一律除外すること、独自閾値で外れ値を削除すること。影響量や個別原因を確認できないものは保留した。 | 新規方針文書、集計スクリプト、個票を含まないmanifest、採用判定基準、文書索引、本判断記録。生データ、胸囲、ANSUR II、モデル、係数、Webアプリ、B/C状態は変更しない。 | 公式コードとの一致、20歳以上6,064件と生コード別2,720/3,344件、分類の相互排他・加算一致、再実行manifest一致、未知コード時停止、禁止処理と個人情報の不存在、B/C保留、生データのGit除外を確認する。 |
| 2026-08-05 | NHANES 2021–2023の第1版成人モデル対象を `RIDAGEYR >= 20` とした。18～19歳は誤りではなく第1版の対象外とし、将来扱う場合は若年成人の別区分として再検討する。`RIAGENDR=1` と `RIAGENDR=2` は生コード別に保持して無条件に結合せず、公式根拠を確認するまで男女などの名称を追加しない。これらの決定だけではBを通過させず、5項目のA可・B保留・C保留を維持する。 | 採用理由は、Aゲート実行2で18歳以上6,337件、20歳以上6,064件、その差273件を確認した上で、法的成人ではなく成人向け身体寸法モデルの第1版対象境界を明確にし、区分別の件数・欠損・QC・評価を維持するため。不採用案は、第1版を18歳以上とする案、18～19歳を誤りとして扱う案、`RIAGENDR` を無条件に結合する案、未確認の名称を生コードへ付与する案である。欠損・QC処理、複雑標本設計、前処理、評価基準と試験結果は未決定であり、B採用の根拠には不足する。詳細件数は基礎集計文書を正本とする。 | `docs/DATA_ACCEPTANCE_CRITERIA.md` と本判断記録のみ。対象抽出方針と統計区分方針に影響するが、今回はデータ処理、集計値、スクリプト、manifest、モデル、推定式、係数、Webアプリを変更しない。NHANES胸囲およびANSUR IIを含むA/B/Cゲート状態も変更しない。 | `git diff --check`、変更ファイルが指定2件だけであること、`RIDAGEYR >= 20` への統一、18～19歳の扱い、`RIAGENDR` 生コード別保持・無条件非結合・名称非付与、5項目のB保留と全項目のB/C状態、Markdown表の列数、ローカルリンクを確認する。将来処理を実装する際は、20歳以上の対象抽出、区分別件数・欠損・QC、ウェイト・層化・クラスタリング、前処理、評価基準と試験結果を再テストする。 |
| 2026-08-05 | Aゲート実行1・2の完了実績を、データ採用判定の正本へ同期した。今回はAゲート実行前の文書矛盾の解消だけを行い、NHANES 5項目のA可・B保留・C保留、NHANES `BMX_L` 胸囲の不可、ANSUR II全項目の保留は変更しない。詳細数値は受入記録と基礎集計文書を正本とし、採用判定基準へ重複コピーしない。 | 採用理由は、Bゲート設計へ進む前に、正本がAゲート実行済みの事実と参照先を示している必要があるため。不採用理由は、今回の同期だけでは成人基準、統計区分、欠損/QC、複雑標本設計、評価基準を決定できず、モデル採用や完成品搭載を正当化できないため。Bゲートへ進む前に、成人基準、統計区分、欠損/QC、複雑標本設計、評価基準を人間が決定する必要がある。 | `docs/DATA_ACCEPTANCE_CRITERIA.md` と本判断記録のみ。受入記録、基礎集計文書、ゲート状態、モデル、推定式、係数、Webアプリ、生データ、scripts、manifestは変更しない。 | `git diff --check`、変更ファイルが2件だけであること、A/B/Cの項目別ゲート状態、NHANES胸囲とANSUR IIの状態、Aゲート実行前の古い記述の不存在、受入記録と基礎集計文書へのローカルリンク、Markdown表の列数、作業後のクリーンなワークツリーを確認する。 |
| 2026-08-05 | Aゲート実行2として、`BMX_L.xpt` と `DEMO_L.xpt` の `SEQN` 欠損・重複・一意件数・共通差分を検査し、安全確認後だけ一対一の限定結合を行った。全年齢、`RIDAGEYR >= 18`、`RIDAGEYR >= 20` を候補として分離し、全体と `RIAGENDR` 生コード別に5項目の非欠損・欠損・利用可能率、関連comment列、`BMDSTATS`、標本設計変数の集計件数だけを保存する。成人基準、男女の結合、欠損・QC処理は決定せず、B・Cを保留のままとする。 | 採用理由は、結合安全性と候補別の事実を個票なしで再現し、Bゲートの人間判断に必要な材料を作れるため。不採用理由は、この件数だけでは成人基準、統計区分、QC除外、複雑標本設計、評価方法を正当化できず、処理やモデルを採用できないため。`WTMEC2YR` の個別値別度数は個票に近く、連続ウェイトに「コード」の意味も確認できないため保存せず、欠損と正・ゼロ・負の集計だけを採用した。 | 集計スクリプト、集計manifest、監査・基礎集計文書、文書索引、判断記録。生・結合済み行データ、ゲート状態、モデル、係数、Webアプリは変更しない。 | Python構文、必須列、`SEQN` 欠損・重複、共通件数と一対一結合件数、年齢・`RIAGENDR` 区分の加算、各項目の非欠損＋欠損＝対象人数、標本設計変数、manifestへの個人値・行データの非収録、生データのGit除外、禁止分析の不存在、再実行時のmanifest一致を確認する。 |
| 2026-08-04 | Aゲート実行1として、`wwwn.cdc.gov` から `BMX_L.xpt` と `DEMO_L.xpt` を取得し、XPT読み込み、行列数、列名、必須列、双方の `SEQN` の存在だけを検査した。生データは `data/raw/` にローカル保存しGit対象外とし、検査manifestと監査記録だけを保存する。成人抽出、結合、欠損・QC処理、前処理決定、統計分析、モデル・推定式・係数、Webアプリには進まず、B・Cの状態を変更しない。 | 採用理由は、Aが可の5項目について公式配布物と既存監査の構造を再現可能に照合し、後工程へ進む前の停止条件を確認できること。生データをコミットしない理由は、今回必要なのは取得・検査の再現手順と証跡であり、Cの搭載・配布可否が未確認のデータ本体をGit履歴に固定・再配布しないためである。不採用理由は、結合や成人抽出以降は今回の目的外であり、Bの処理方針も未決定だからである。この扱いは将来の搭載・配布可否を決定しない。 | ダウンロード・検査スクリプト、依存関係、`.gitignore`、機械可読manifest、受入監査記録、NHANES監査の時点注記、文書索引。生データ本体、ゲート判定、製品仕様、モデル、係数、Webアプリには変更なし。 | 許可ホスト制限、再ダウンロード、XPT読み込み、サイズとSHA-256、行列数と全列名、必須列、両ファイルの`SEQN`、manifest整合、受入文書との一致、生データがGit管理対象外であること、結合・成人抽出・統計処理がないことを確認する。次工程前にはBの許可範囲、成人抽出、欠損・QC、複雑標本設計の決定条件を再確認する。 |
| 2026-08-04 | 作業ルールの「外部データのダウンロードや分析は、データの採用判断後に行う」を、項目別の3段階ゲートに対応する規則へ置き換えた。Aが可の項目だけ公式取得元からの生データ取得、内容確認、限定的な前処理検討、試験的統計分析を許可し、B通過前のモデル・推定式・係数の正式採用と、C通過前の完成品搭載・公開・商用利用・再配布を禁止する。現在の対象はNHANES `BMX_L` の身長・体重・BMI・ウエスト・ヒップだけとし、同候補の胸囲はA不可、ANSUR II全項目は保留を維持する。 | 旧ルールの「採用判断」がA・B・Cのどの判定を指すか不明確で、Aを通過した限定解析まで止める解釈と、A通過を正式採用と誤解して後続工程へ進む解釈の両方が生じ得たため。新ルールの採用理由は、許可する工程と停止条件をゲートごとに明示し、既存の項目別判定どおりに次工程を制御できること。不採用理由は、単一の「採用判断」を前提にした旧ルールでは3ゲートを区別できないこと。 | `AGENTS.md` の作業制約と本判断記録のみ。データ採用判定基準のゲート判定、製品仕様、README、文書索引、監査記録、データ、モデル、推定式、係数、Webアプリには変更なし。今回は取得・分析を実施しない。 | Aで許可する作業が限定列挙されていること、A通過がB・C通過を意味しないこと、B・Cの停止規則、現在の項目別状態が採用判定基準と一致すること、Markdown表と文書リンク、変更範囲外に差分がないことを確認する。 |
| 2026-07-31 | 単一の採用状態を「解析開始」「モデル採用」「完成品搭載・配布」の3ゲートへ分割した。NHANES `BMX_L` の身長・体重・BMI・ウエスト・ヒップは解析開始可、後続2ゲートは保留とし、同候補の胸囲は全ゲート不可とした。ANSUR IIは胸囲候補に残すが全項目の解析開始を保留する。第0.1版では胸囲を自動推定せず、手入力は固定保持、空欄は補完しない。自動推定候補はウエストとヒップとし、BMI自動計算方針は維持する。 | 監査基準を緩めるのではなく、内容確認・試験分析に必要な条件と、モデル採用・完成品配布に必要な後工程の条件を分離するため。NHANESの5項目は既存監査で取得元、正式変数、単位、測定または派生定義、欠損・QC概要を確認済みで解析着手の根拠がある一方、処理・評価・利用条件は未確認で正式採用できない。胸囲は `BMX_L` に存在せず、ANSUR IIも公式変数説明、正式取得元、定義、単位、欠損・QC、利用条件が不足するため、根拠のない推定を避ける。ANSUR IIを残す理由は米陸軍記事で `chest circumference` の存在のみ確認済みだからであり、解析開始保留の理由は存在確認だけでは不足するため。男女データは無条件に結合しない。 | データ採用判定基準、第0.1版の入力保持・予定出力、READMEと文書索引。監査記録、確認済み事実、未確認事項、不採用理由、生データには変更なし。モデル、推定式、係数、Webアプリは作成しない。胸囲の予定出力から自動推定対象を除外するが、入力欄と固定チェックは保持する。 | Markdown表、項目別3ゲート状態、NHANES胸囲を当該候補だけ不可としていること、ANSUR II全項目の解析開始保留と男女非結合、胸囲の手入力固定・空欄保持、予定出力から胸囲推定を除外、BMI自動計算、変更対象外ファイルに差分がないことを確認する。 |
| 2026-07-30 | 文書索引を追加し、詳細の正本は各担当文書とした。総合仕様書・完成仕様書は後日、マイルストーン時点のスナップショットとして作成する。データ採用判定ゲートも追加し、通過するまでモデル・係数・Webアプリへ進まない。 | 文書名を覚えなくても役割と正本を探せるようにし、スナップショットと更新される担当文書の食い違いを無断で統合しないため。データセット一括ではなく測定項目ごとに、存在確認と定義確認を分け、未確認の利用条件や標本の限界を残したまま実装へ進むことを防ぐため。採用理由は確認済み証拠を項目別に追跡できること。不採用（現時点）理由はNHANES、ANSUR IIとも必須条件が未充足であること。 | 文書索引、READMEの文書案内、データ採用判定、判断記録のみ。既存の仕様、監査内容、モデル、係数、Webアプリ、生データには変更なし。 | INDEXに全担当文書と正本が掲載されていること、採用状態が4段階であること、6項目×2候補の不足証拠・次の判断・停止条件が記載され、両候補が正式採用になっていないことを確認する。 |
| 2026-07-30 | 現行URLと米陸軍公式記事でANSUR IIを再監査した。胸囲候補として変数説明の再監査対象には残すが、正式採用せず、男女データを無条件に結合しない。 | 米陸軍記事から2012 Army-wide Anthropometric Survey、Soldiers、男女別件数、93 body measurements、`chest circumference`等の存在は確認できた。一方、現行Anthropometric DatabaseページはHTTP 503で、測定定義、単位、欠損・QC、利用条件を確認できない。候補として残す理由は胸囲の存在が公式確認できたこと、正式採用しない理由は必要な定義と条件が未確認で、軍人標本を一般成人へ一般化できないこと。 | 文書のみ。仕様、モデル、係数、Webアプリ、生データには変更なし。 | 現行公式ページ、Memorandum、変数説明への接続を再試行し、胸囲の呼吸相・姿勢・位置、保存単位、欠損/QC、男女別構造、利用条件、NHANESとの整合を確認する。 |
| 2026-07-29 | ANSUR IIは正式採用せず監査中とし、男性データと女性データを無条件に結合しない。 | 許可された公式ドメインだけで第2段階監査を行ったが、Defense Centers for Public Healthの公式ページが監査環境でHTTP 503となり、Memorandum、変数説明、米陸軍記事を検証できなかった。変数、測定定義、保存単位、対象集団、件数、利用条件を推測で補えないため、採用理由は現時点で確認できず、不採用（現時点）理由は採用判断に必要な公式根拠が未確認であること。男女を結合しない理由は、ファイル構造、測定定義、標本設計、対象集団の差が未確認であること。 | 文書（データ候補表、ANSUR II監査、判断記録）のみ。仕様、モデル、係数、Webアプリ、生データには変更なし。 | 公式ページ、Memorandum for Record、変数説明、米陸軍記事への接続を再試行し、変数名・単位・定義・男女別件数・対象集団・欠損/QC・利用条件・引用方法・NHANESとの整合を再監査する。 |
| 2026-07-28 | NHANES August 2021–August 2023 は正式採用せず、データ出典候補として監査中を維持する。今回は ANSUR II を調査していない。 | 公式資料で身長・体重・計算BMI・ウエスト・ヒップ、測定定義、標本ウェイトの必要性は確認できた。一方、`BMX_L` に胸囲は確認できず、許可ドメイン内で Data User Agreement 本文を確認できず、アバター用途の精度・一般化可能性も未評価である。採用理由としては標準手順と結合可能な項目があること、不採用（現時点）理由としては未確認事項と仕様上必要な胸囲の不足がある。 | データ候補文書と監査記録のみ。仕様、モデル、係数、UI、Webアプリ、生データには変更なし。 | 公式利用条件の確認、胸囲候補との定義整合、成人部分集合・欠損・複雑標本設計を含む分析計画、採用基準の確定後に再監査する。 |
| 未確定 | 未確定 | 未確定 | 未確定 | 未確定 |

## 記入ルール

- 日付は `YYYY-MM-DD` 形式で記録する。
- 変更理由には、採用理由だけでなく不採用理由も必要に応じて記録する。
- 影響範囲には、仕様、データ、モデル、UI、テストへの影響を記録する。
- 再テスト項目には、変更後に確認すべきテストを具体的に記録する。
